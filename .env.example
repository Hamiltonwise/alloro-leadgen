# Backend API base URL (optional; falls back to app.getalloro.com/api in prod,
# http://localhost:3000/api in dev)
VITE_API_BASE_URL=

# Leadgen tracking — shared secret for the public tracking endpoint
VITE_LEADGEN_TRACKING_KEY=local-dev-key-change-me
